{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "zensu-agent.serviceAccountName" . }}
  labels:
    {{- include "zensu-agent.labels" . | nindent 4 }}
{{- end }}
