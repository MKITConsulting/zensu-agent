{{- if and (eq .Values.agent.mode "deployment") .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zensu-agent.fullname" . }}-metrics
  labels:
    {{- include "zensu-agent.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "zensu-agent.selectorLabels" . | nindent 4 }}
  ports:
    - name: metrics
      port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
{{- end }}
